#!/bin/bash

mysql_host="{{ hostvars[groups['tidb_servers'][0]].ansible_host }}"
mysql_port="3306"
mysql_user="$1"
mysql_password="$2"

mysql -h ${mysql_host} -P ${mysql_port} -u ${mysql_user} -p${mysql_password} -s -e < ./ds-ddl-1.3.1.sql

mysql -h ${mysql_host} -P ${mysql_port} -u ${mysql_user} -p${mysql_password} -s -e < ./ds-dml-1.3.1.sql